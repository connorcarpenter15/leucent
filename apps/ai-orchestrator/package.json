{
  "name": "@leucent/ai-orchestrator",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn app.main:app --reload --host 0.0.0.0 --port 5000",
    "build": "python -m compileall app",
    "lint": "ruff check app",
    "typecheck": "echo 'mypy skipped (optional)'",
    "test": "pytest -q"
  }
}
